# The native addon belongs to razer-macos / librazermacos (GPL) — not shipped here.
# Run cli/setup.sh (or app setup) to extract it from your installed Razer app.
addon.node
cli/addon.node
app/addon.node

# Dependencies and build output
node_modules/
app/dist/
app/out/
app/build/

.DS_Store
